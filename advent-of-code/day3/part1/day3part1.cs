using System;
using System.IO;

namespace day3part1
{
    class Hello {         
        static void Main()
        {
            //open the file
            //store contents in an array of strings
            //get the number of strings in the array and the length of each string
            //loop through first 3 chars in the first string (start at char[0])
            //move to the next string in the array to the charcter same as in the previouse
            //continue until the end of string reached
            //then move back to char[0] in the next string

            //when # ecountered, count++



        }
    }
}